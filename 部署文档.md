# 江东ERP 本地部署详细步骤

## 一、项目概述

江东ERP是一款面向中小企业的进销存管理系统，采用前后端分离架构。

- **前端**：Vue 2 + Ant Design Vue
- **后端**：Spring Boot 2.0 + MyBatis-Plus
- **数据库**：MySQL 8.0
- **缓存**：Redis
- **后端端口**：9999
- **前端端口**：3000（开发环境）

## 二、环境要求

| 软件 | 版本要求 | 说明 |
|------|----------|------|
| JDK | 1.8 及以上 | 推荐 JDK 8 或 JDK 17 |
| Maven | 3.6 及以上 | 项目构建工具 |
| Node.js | 12.x 及以上 | 推荐 v16+ |
| MySQL | 5.7 或 8.0 | 数据库 |
| Redis | 5.0 及以上 | 缓存服务 |

## 三、部署步骤

### 3.1 数据库准备

1. **创建数据库**

```sql
CREATE DATABASE jsh_erp DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

2. **导入初始数据**

数据库脚本位于 `jshERP-boot/docs/jsh_erp.sql`

```bash
mysql -u root -p jsh_erp < jshERP-boot/docs/jsh_erp.sql
```

3. **修改系统名称（可选）**

```sql
USE jsh_erp;
UPDATE jsh_platform_config SET platform_value='江东ERP' WHERE platform_key='platform_name';
```

### 3.2 后端部署

1. **修改配置文件**

配置文件路径：`jshERP-boot/src/main/resources/application.yml`

```yaml
server:
  port: 9999
  servlet:
    context-path: /jshERP-boot

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/jsh_erp?useUnicode=true&characterEncoding=utf8&useCursorFetch=true&defaultFetchSize=500&allowMultiQueries=true&rewriteBatchedStatements=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 你的数据库密码
  redis:
    host: 127.0.0.1
    port: 6379
    password: 你的Redis密码（没有则留空）
```

2. **编译项目**

```bash
cd jshERP-boot
mvn clean package -DskipTests
```

编译成功后，在 `target/` 目录下会生成 `jshERP.jar`

3. **启动后端服务**

方式一：使用 Maven 启动（开发环境）
```bash
cd jshERP-boot
mvn spring-boot:run
```

方式二：使用 Jar 包启动（生产环境）
```bash
cd jshERP-boot/target
java -jar jshERP.jar
```

方式三：后台运行
```bash
nohup java -jar jshERP.jar > app.log 2>&1 &
```

4. **验证后端启动**

访问 API 文档：http://localhost:9999/jshERP-boot/doc.html

### 3.3 前端部署

1. **安装依赖**

```bash
cd jshERP-web
npm install
```

如果安装缓慢，可使用淘宝镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

2. **修改配置（可选）**

配置文件路径：`jshERP-web/vue.config.js`

```javascript
devServer: {
    port: 3000,  // 前端端口
    proxy: {
        '/jshERP-boot': {
            target: 'http://localhost:9999',  // 后端地址
            ws: false,
            changeOrigin: true
        }
    }
}
```

3. **启动开发服务**

```bash
cd jshERP-web
npm run serve
```

启动成功后访问：http://localhost:3000

4. **生产环境构建**

```bash
cd jshERP-web
npm run build
```

构建完成后，`dist/` 目录下的文件可以部署到 Nginx 等 Web 服务器。

### 3.4 Nginx 部署（生产环境推荐）

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态资源
    location / {
        root /path/to/jshERP-web/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后端API代理
    location /jshERP-boot/ {
        proxy_pass http://127.0.0.1:9999/jshERP-boot/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## 四、登录信息

| 用户名 | 密码 | 说明 |
|--------|------|------|
| jsh | 123456 | 租户管理员（推荐使用） |
| admin | 123456 | 平台运维用户，不能编辑业务数据 |

## 五、常见问题

### 5.1 端口被占用

```bash
# 查看端口占用
lsof -i :9999
# 或
netstat -tlnp | grep 9999

# 杀掉占用进程
kill -9 <PID>
```

### 5.2 数据库连接失败

- 确认 MySQL 服务已启动
- 检查数据库地址、端口、用户名、密码是否正确
- 确认数据库 `jsh_erp` 是否已创建

### 5.3 Redis 连接失败

- 确认 Redis 服务已启动：`redis-cli ping`
- 检查 Redis 地址、端口、密码配置

### 5.4 前端启动失败

- 删除 `node_modules` 后重新安装依赖
- 检查 Node.js 版本是否符合要求
- 确认端口 3000 未被占用

### 5.5 JDK 版本兼容问题

如果使用 JDK 17 及以上版本，需要添加以下 JVM 参数：

```bash
java --add-opens java.base/java.lang=ALL-UNNAMED \
     --add-opens java.base/java.lang.reflect=ALL-UNNAMED \
     -jar jshERP.jar
```

## 六、目录结构说明

```
/workspace
├── jshERP-boot/              # 后端项目
│   ├── src/
│   │   └── main/
│   │       ├── java/         # Java 源码
│   │       └── resources/    # 配置文件
│   ├── docs/
│   │   └── jsh_erp.sql       # 数据库脚本
│   ├── pom.xml               # Maven 配置
│   └── target/               # 编译输出
├── jshERP-web/               # 前端项目
│   ├── src/                  # 源码
│   ├── public/               # 静态资源
│   ├── package.json          # 依赖配置
│   └── vue.config.js         # Vue 配置
└── mise.toml                 # 开发环境配置
```

## 七、品牌自定义

如需修改系统品牌名称，需要修改以下位置：

1. **数据库**：`jsh_platform_config` 表中 `platform_name` 字段
2. **前端页面**：
   - `jshERP-web/public/index.html` - 多语言翻译配置
   - `jshERP-web/src/components/layouts/UserLayout.vue` - 登录页底部
   - `jshERP-web/src/components/tools/UserMenu.vue` - 用户菜单广告
   - `jshERP-web/src/views/dashboard/IndexChart.vue` - 首页
3. **后端配置**：
   - `jshERP-boot/pom.xml` - 项目描述
   - `jshERP-boot/src/main/java/com/jsh/erp/config/Swagger2Config.java` - API文档标题
